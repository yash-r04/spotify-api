SPOTIFY API project
It runs on a flask back end server.
It authorises the user and fetches user data - top 10 artist, tracks and also recommends songs.
The user can choose the one of the 5 genre to generate a list of songs
